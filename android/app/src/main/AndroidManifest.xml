<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="io.github.borctakipyonetimi">

    <!-- İnternet ve WebView Ağ Erişim İzinleri -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Android 13+ (API 33+) Sistem Çekmecesi Bildirim Gösterme İzni -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Android 10+ / 14+ Kilit Ekranından Aşağı İnen Heads-Up Bildirim ve Tam Ekran Alarm İzni -->
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />

    <!-- Telefon Ekranını Fiziksel Olarak Açma ve Kilit Ekranını Uyandırma İzinleri -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.TURN_SCREEN_ON" tools:ignore="ProtectedPermissions" />
    <uses-permission android:name="android.permission.DISABLE_KEYGUARD" />

    <!-- Android 12+ (API 31+) ve Android 14+ Tam Zamanlı Arka Plan Alarm İzinleri -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />

    <!-- Donanımsal Titreşim İzni -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- Telefon Yeniden Başlatıldığında (Reboot) Alarmları Otomatik Geri Yükleme İzni -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- Dosya İndirme ve Dışa Aktarma İzinleri -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />

    <!-- Google Drive, WhatsApp ve Sistem Paylaşımı için Paket Görünürlüğü (Android 11+) -->
    <queries>
        <package android:name="com.google.android.apps.docs" />
        <package android:name="com.whatsapp" />
        <package android:name="org.telegram.messenger" />
        <intent>
            <action android:name="android.intent.action.SEND" />
            <data android:mimeType="*/*" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
    </queries>

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_stat_alarm"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_stat_alarm"
        android:supportsRtl="true"
        android:theme="@style/Theme.BorcTakip"
        android:usesCleartextTraffic="true"
        tools:targetApi="34">

        <!-- Ana WebView Ekranı -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:showWhenLocked="true"
            android:turnScreenOn="true"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Kilit Ekranında ve Ekran Kapalıyken Üstten Açılan Acil Alarm Penceresi -->
        <activity
            android:name=".AlarmAlertActivity"
            android:exported="false"
            android:showWhenLocked="true"
            android:turnScreenOn="true"
            android:excludeFromRecents="true"
            android:launchMode="singleTop"
            android:theme="@style/Theme.BorcTakip" />

        <!-- Arka Planda Borç Hatırlatıcılarını Yakalayıp Sesli/Titreşimli Alarm Üreten Receiver -->
        <receiver
            android:name=".DebtAlarmReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="io.github.borctakipyonetimi.ACTION_DEBT_ALARM" />
                <action android:name="io.github.borctakipyonetimi.ACTION_CHECK_OVERDUE_DEBTS" />
            </intent-filter>
        </receiver>

        <!-- Üst Çekmecedeki 'Kapat' Butonu Eylemini Yakalayan Receiver -->
        <receiver
            android:name=".DebtAlarmDismissReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="io.github.borctakipyonetimi.ACTION_DISMISS_ALARM" />
            </intent-filter>
        </receiver>

        <!-- Telefon Yeniden Başlatıldığında Kayıtlı Alarmları AlarmManager'a Yeniden Kuran Receiver -->
        <receiver
            android:name=".BootReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>

        <!-- Yerel Yedek Dosyalarını WhatsApp, Google Drive ve Diğer Uygulamalarla Paylaşma Sağlayıcısı -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>
</manifest>
